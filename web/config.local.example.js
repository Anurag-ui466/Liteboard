// Copy this file to config.local.js (gitignored) and fill in a test account to
// enable dev auto-login locally. config.js imports it with a safe fallback, so
// if config.local.js is absent, auto-login is simply OFF and the login screen shows.
export const DEV_AUTOLOGIN = true;
export const DEV_EMAIL = "you@example.com";
export const DEV_PASSWORD = "your-test-password";
